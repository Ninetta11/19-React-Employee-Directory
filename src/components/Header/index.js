function Header() {
    return (
        <div class="jumbotron jumbotron-fluid">
            <div class="container">
                <h1 class="display-4">Employee Directory</h1>
                <p class="lead">Click on controls to filter by heading or use the search bx to narrow your results.</p>
            </div>
        </div>
    );
}

export default Header;
